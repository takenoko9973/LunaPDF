# 性能測定手順

設計書15.4の性能目標を、同じ入力と操作列で再現して記録する。測定時に環境、方法、結果、判定対象を追記する。

## 固定fixtureと操作列

- fixture: 50–100ページの通常PDFを毎回使用する。20タブ測定では同じ内容を持つ別パスの20ファイルを用意し、各PDFのページ数、ファイルサイズ、画像の有無、checksumをmanifestへ記録する。同じパスは既存タブへ集約されるため20タブ測定には使わない。
- cold: 測定環境で再現可能な方法によりアプリとOSのファイルキャッシュを空にし、その方法を記録してから最初のPDFを開く。キャッシュ状態を確認できない測定はwarmとして扱う。
- warm: アプリを終了・再起動するが、OSのファイルキャッシュは維持して同じPDFを開く。
- 操作列: 起動 → PDFを開く → 最初のページが表示されるまで待つ → 3ページ分スクロール → 検索語を入力してUIの応答を測る → 検索完了を待つ → manifest順に20ファイルを開く。
- 1/10/20タブの各時点で、非アクティブタブを操作せずにRSSを記録する。

## 環境

測定ごとに次を記録する。

| 項目 | 値 |
| --- | --- |
| commit | |
| OS / version | |
| CPU / RAM | |
| GPU / driver | |
| display scale (DPI) | |
| build profile | `release` / `debug` |
| fixture manifest checksum | |

## RSS測定（2026-07-18、3回）

20タブ時のメモリ使用量をDev ContainerのWSLg環境で3回測定した。この結果はN-05のRSSだけを判定し、起動時間、UI応答性、GPUアクセラレーション環境、cache plateauなど未測定の要件には使用しない。

| 項目 | 値 |
| --- | --- |
| commit | `82619f9` |
| OS / version | Debian GNU/Linux 13.6（Docker Desktop / WSLg） |
| CPU / RAM | AMD Ryzen 7 9700X / コンテナ認識15.6 GiB |
| GPU / driver | Mesa 25.0.7、llvmpipe LLVM 19.1.7（ソフトウェア描画） |
| display scale (DPI) | X11論理解像度96×96 DPI（5120×1440 px） |
| build profile | `release` |
| fixture | qpdf 11.9.1 manualの先頭100ページ、592,972 bytes |
| fixture SHA-256 | `EE33A0DBB46B609A36C8AC6E93BA38CD684F5A240E8582A85B010974308D85DB` |

同一内容を別パスへ複製し、コマンドラインから1、10、20ファイルを開いた。非アクティブタブは操作せず、すべての文書ワーカーが起動した安定状態でLinuxの`/proc/<pid>/status`からRSSを取得した。

| タブ数 | 安定待機 | run 1 | run 2 | run 3 | 中央値 | 最大値 | threads |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 10 s | 152.7 MiB | 159.5 MiB | 154.6 MiB | 154.6 MiB | 159.5 MiB | 21 |
| 10 | 15 s | 158.4 MiB | 160.1 MiB | 164.3 MiB | 160.1 MiB | 164.3 MiB | 30 |
| 20 | 20 s | 172.0 MiB | 173.0 MiB | 172.6 MiB | 172.6 MiB | 173.0 MiB | 40 |

20タブのRSSは3回とも暫定目標512 MiB以内だった。ただし、各タブのスクロール後、GPUアクセラレーション環境、cache plateau、起動時間、フレーム時間、検索入力遅延は未測定である。

## warm起動測定（2026-07-19、3回）

固定fixtureをコマンドライン引数に指定し、Dev ContainerのWSLg環境でwarm起動時間を3回測定した。この結果はN-04のwarm起動だけを判定し、cold起動、初回ページ表示、UI応答性には使用しない。

| 項目 | 値 |
| --- | --- |
| commit | `e6c3be9` |
| OS / version | Debian GNU/Linux 13.6（Docker Desktop / WSLg） |
| CPU / RAM | AMD Ryzen 7 9700X / コンテナ認識15.6 GiB |
| GPU / driver | Mesa 25.0.7、llvmpipe LLVM 19.1.7（ソフトウェア描画） |
| display scale (DPI) | X11論理解像度96×96 DPI（5120×1440 px） |
| build profile | `release` |
| fixture | qpdf 11.9.1 manualの先頭100ページ、592,972 bytes |
| fixture SHA-256 | `EE33A0DBB46B609A36C8AC6E93BA38CD684F5A240E8582A85B010974308D85DB` |

releaseビルド後に1回起動してOSのファイルキャッシュをwarmにし、その試行は集計から除外した。各測定では前のプロセスが終了したことを確認してから同じPDFを引数に再起動した。Wayland経路との差を混ぜないため、`WAYLAND_DISPLAY`を外して`WINIT_UNIX_BACKEND=x11`を指定した。

測定開始点はプロセス起動直前、終了点は`xprop -name LunaPDF WM_STATE`が最初に`Normal`を返した時点とした。状態は2 ms間隔で確認した。除外したwarm-upは53 msだった。

| run 1 | run 2 | run 3 | 中央値 | 最大値 | 判定 |
| ---: | ---: | ---: | ---: | ---: | --- |
| 54 ms | 57 ms | 51 ms | 54 ms | 57 ms | pass |

3回ともN-04の暫定目標1秒以内だった。ただし、`WM_STATE=Normal`はウィンドウが表示可能になったことだけを示し、PDFの最初のページが描画済みであることは示さない。

## X11応答・描画測定（2026-07-19、3回）

固定fixtureをコマンドライン引数に指定し、現在のcommitでウィンドウ表示、最初のPDF内容、ページ移動、検索入力、連続スクロールを外部計測した。この系列では画面画像を保存せず、X11から対象ウィンドウの限定領域をメモリへ読み、画素ハッシュと変化時刻だけを記録した。

| 項目 | 値 |
| --- | --- |
| commit | `f902813` |
| OS / version | Debian GNU/Linux 13.6（Docker Desktop / WSLg） |
| CPU / RAM | AMD Ryzen 7 9700X / コンテナ認識15.6 GiB |
| GPU / driver | Mesa 25.0.7、llvmpipe LLVM 19.1.7（ソフトウェア描画） |
| display scale (DPI) | X11論理解像度96×96 DPI、測定ウィンドウ1029×771 px |
| build profile | `release` |
| fixture | qpdf 11.9.1 manualの先頭100ページ、592,972 bytes |
| fixture SHA-256 | `EE33A0DBB46B609A36C8AC6E93BA38CD684F5A240E8582A85B010974308D85DB` |
| 外部計測器 | `xdotool` 3.20160805.1、ImageMagick、Xlib小領域watcher |

起動と最初のページ表示では、プロセス起動直前を開始点とした。ウィンドウ表示後、ツールバー、status、ページ枠、スクロールバーを除いた850×500 pxを観測した。固定fixtureの描画済み領域は暗画素率1.17412%であり、0.2%から10%だけを有効としたため、空白プレースホルダーとX11の一時的な全黒バッファは完了判定にならない。画素取得と判定が終わった時刻を終了点にするため、最初のPDF内容表示は実表示時刻の上限値である。

| 測定 | run 1 | run 2 | run 3 | 中央値 | 最大値 | 判定 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| warmウィンドウ表示 | 58.657 ms | 59.505 ms | 74.998 ms | 59.505 ms | 74.998 ms | pass |
| warm最初のPDF内容 | 336.914 ms | 395.441 ms | 432.953 ms | 395.441 ms | 432.953 ms | pass |
| ウィンドウ表示後からPDF内容 | 278.257 ms | 335.935 ms | 357.956 ms | 335.935 ms | 357.956 ms | 内訳 |

ページ移動は、意図的な空白ページである2ページ目から、1,835文字を持つ3ページ目への移動に固定した。移動前と異なる画素ハッシュ、暗画素率0.2%から10%、同じ画素ハッシュが2回連続し、その100 ms後も同じ、という条件をすべて満たした時刻を可視タイル完成とした。100 msの遅延確認を含むため、これも完成時刻の上限値である。3回の完成画像は同じSHA-256 `2F44C5F8C92378DF35C383D0170D7E85410B3E4BA4EBFDA52F53312261FA8532`だった。

| run 1 | run 2 | run 3 | 中央値 | 最大値 | 判定 |
| ---: | ---: | ---: | ---: | ---: | --- |
| 410.139 ms | 424.177 ms | 402.988 ms | 410.139 ms | 424.177 ms | 基準値を記録 |

入力遅延とフレーム間隔には、Xlibの`XGetImage`で小領域を1 ms間隔で読み、FNV-1aハッシュが変化した実時刻を出す外部watcherを使用した。検索欄はFindボタンと検索欄内部をポインター操作してフォーカスし、`object`を入力した。各試行で入力後16回の検索欄・進捗表示変化を観測した。連続スクロールは文字量が連続して多い15ページから18ページを対象とし、20 ms間隔で45回のホイール入力を送り、文書領域の画素変化間隔を測った。

| 測定 | run 1 | run 2 | run 3 | 中央値 | 最大値 | 判定 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| 検索入力から最初の画面反映 | 17.471 ms | 17.092 ms | 16.316 ms | 17.092 ms | 17.471 ms | pass |
| 最初のスクロール反映 | 20.493 ms | 21.584 ms | 19.826 ms | 20.493 ms | 21.584 ms | pass |
| 連続スクロール中の最大画面更新間隔 | 52.428 ms | 58.183 ms | 52.693 ms | 52.693 ms | 58.183 ms | pass |
| 観測したスクロール画面更新数 | 149 | 153 | 152 | 152 | 153 | 補助値 |

検索入力と連続スクロールは、いずれもN-02の100 msを超える停止を観測しなかった。X11画素変化を外部観測した結果であり、GPUのPresent完了時刻そのものではない。

## 対象ファイルcold測定（2026-07-19、3回）

コンテナには`/proc/sys/vm/drop_caches`への書き込み能力がないため、システム全体のOS file cacheを空にした測定は実施できなかった。代わりに`vmtouch` 1.3.1で、各試行直前にLunaPDF releaseバイナリと固定fixtureの計4,241ページ、約16 MiBを退避し、常駐ページが`0/4241`であることを確認してから起動した。

| 測定 | run 1 | run 2 | run 3 | 中央値 | 最大値 | 判定 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| 対象ファイルcoldウィンドウ表示 | 80.709 ms | 85.309 ms | 79.056 ms | 80.709 ms | 85.309 ms | 参考値 |
| 対象ファイルcold最初のPDF内容 | 372.123 ms | 401.478 ms | 379.820 ms | 379.820 ms | 401.478 ms | 参考値 |
| ウィンドウ表示後からPDF内容 | 291.413 ms | 316.169 ms | 300.764 ms | 300.764 ms | 316.169 ms | 内訳 |

この限定条件ではN-03の500 msとN-04の1秒以内だった。ただし、共有ライブラリ、フォント、X11/GLドライバーを含むシステム全体のfile cacheは退避していないため、設計書15.4の完全なcold測定を通過したとは判定しない。

## cache plateau測定（2026-07-19）

固定fixtureと同じ内容を持つ別パス20個を開いた。20ファイルはいずれも592,972 bytes、100ページで、SHA-256は同一だった。

再測定対象はcommit `49d8aa1`である。このcommitではeguiへのupload後に`std::mem::take`でworker転送用RGBA allocationを解放する。`rgba_upload_releases_the_worker_transfer_allocation`テストで、upload後のpayloadがlength 0かつcapacity 0になることを確認した。

CPU側ではRGBA画像を永続キャッシュせず、各文書ワーカーのbounded event channelと描画中の一時バッファだけを保持する。512 px RGBAタイルは最大1 MiBであり、1ワーカー最大9 MiB、20ワーカー合計180 MiB以下になる。GPU textureは共有192 MiB、thumbnailは共有32 MiBのバイト重みLRUである。各LRUが挿入後に上限を超えないことは`WeightedLruCache`の単体テストでも確認している。

GPU LRUは、20タブを`Ctrl+Tab`で一巡した後、同じページを異なるzoom generationで順に描画して到達させた。アプリのstatus文字列を、画像を保存せずOCRした。20タブ一巡後は`126 tiles (126.0/192 MiB)`、zoom-in step 5で`195 tiles (190.2/192 MiB)`だった。その後の逆方向10段階では、distinctなcache keyを追加しながら`198–212 tiles (191.1–191.9/192 MiB)`を維持した。上限近傍で新規挿入を続けても192 MiBを超えないため、LRU削除が発生したことを確認できる。LunaPDFのRSSは403.586 MiBで、入れ替え直後、2秒、5秒後まで同じだった。

このGPU試行は、20タブを開いたまま極端な連続zoomで192 MiBへ強制到達させるstress workloadであり、通常操作のN-05判定には使わない。通常の20タブ安定時RSSは前節の最大173.0 MiBを判定値とする。

thumbnailは全100ページが612×792 ptのfixtureを使った。160×208 px RGBAとして1枚133,120 bytes、3文書300枚で39,000 KiB（38.086 MiB）相当を走査するため、共有32 MiB予算を超えてLRU入れ替えが発生する操作列である。

| 時点 | RSS | peak RSS |
| --- | ---: | ---: |
| 初期20タブ | 168.164 MiB | 172.750 MiB |
| 1文書100ページ走査後 | 180.277 MiB | 180.277 MiB |
| 2文書200ページ走査後 | 180.277 MiB | 180.277 MiB |
| 3文書300ページ走査後 | 180.277 MiB | 180.277 MiB |
| 同じ300ページを再走査した2周目 | 180.277 MiB | 180.277 MiB |
| 同じ300ページを再走査した3周目 | 180.277 MiB | 180.277 MiB |
| 3周目直後 / 2秒 / 5秒待機 | 180.277 MiB | 180.277 MiB |

GPU LRUとthumbnail LRUは、いずれも予算到達後の入れ替え操作でRSSが増え続けなかった。WSLgのドライバー側メモリは別計測していないため、この結果はLunaPDFプロセスRSSとアプリ内のバイト予算だけを判定する。

## 記録する値

status表示で直接読める値はアプリの表示値を使い、RSS・フレーム時間・入力遅延は外部計測器で測る。各値はcold/warmを分け、単位を固定する。

| 測定 | 取得方法 | cold | warm | 目標 / 判定 |
| --- | --- | ---: | ---: | --- |
| 起動時間 | プロセス開始からウィンドウ表示までを外部計測 | 80.709 ms（対象ファイルcold） | 59.505 ms | 1 s以内を目標。完全OS coldは未測定 |
| 初回ページ表示 | open操作から最初のページ表示までを外部計測。statusのopen時間は内訳として併記 | 379.820 ms（対象ファイルcold） | 395.441 ms | 500 ms以内を目標。完全OS coldは未測定 |
| 可視タイル完成 | ページ移動から可視タイル完成までを外部計測。statusのrender時間は内訳として併記 |  | 410.139 ms | 基準値を記録 |
| RSS: 1 tab | 外部プロセス計測 |  | 154.6 MiB | 推移を記録 |
| RSS: 10 tabs | 外部プロセス計測 |  | 160.1 MiB | 推移を記録 |
| RSS: 20 tabs | 外部プロセス計測 |  | 172.6 MiB | 安定時512 MiB以内を目標 |
| 最大フレーム時間 | 外部画素変化計測 |  | 52.693 ms | 通常操作で100 ms超の停止なし |
| 検索入力遅延 | 検索中の入力からUI反映までを外部計測 |  | 17.092 ms | 100 ms超の停止なし |
| cache plateau後のRSS推移 | 予算到達後に外部計測 |  | GPU stress 403.586 MiB、thumbnail 180.277 MiBで横ばい | CPU転送180 MiB以下、GPU texture 192 MiB、thumbnail 32 MiBの各上限後に増加し続けない |

`open`、`render`、`text`、`resident memory` は画面のstatus表示から直接読める。起動時間、初回ページ表示、可視タイル全体の完成、RSS、フレーム時間、検索入力遅延、cache plateauの判定は外部計測が必要である。

## 結果表

同じfixture・環境で複数回測定し、中央値と最大値を記録する。実測値がない欄は空欄のままにし、推測値で補完しない。

| 測定 | 回数 | 中央値 | 最大値 | pass/fail | 備考 |
| --- | ---: | ---: | ---: | --- | --- |
| 完全OS cold起動 |  |  |  | 未判定 | コンテナからdrop_caches不可 |
| 対象ファイルcold起動 | 3回 | 80.709 ms | 85.309 ms | 参考 | バイナリとfixtureの常駐0/4241ページを確認。共有ライブラリ等はwarm |
| warm起動 | 3回 | 54 ms | 57 ms | pass | X11の`WM_STATE=Normal`まで。PDF引数あり、release、warm-up除外 |
| warm起動（現行commitの画素測定系列） | 3回 | 59.505 ms | 74.998 ms | pass | 対象ウィンドウをX11で外部観測 |
| warm初回ページ表示 | 3回 | 395.441 ms | 432.953 ms | pass | PDF内容画素の出現まで。取得・判定を含む上限値 |
| 対象ファイルcold初回ページ表示 | 3回 | 379.820 ms | 401.478 ms | 参考 | 完全OS coldではない |
| 可視タイル完成 | 3回 | 410.139 ms | 424.177 ms | 基準値 | 100 ms後の同一画素確認を含む上限値 |
| RSS 1/10/20 tab | 各3回 | 154.6 / 160.1 / 172.6 MiB | 159.5 / 164.3 / 173.0 MiB | pass | 100ページ技術資料、release、非アクティブタブ未操作 |
| 最大画面更新間隔 | 3回 | 52.693 ms | 58.183 ms | pass | 15〜18ページを20 ms間隔で連続スクロール |
| 検索入力遅延 | 3回 | 17.092 ms | 17.471 ms | pass | 各回16回の検索進捗画面変化を観測 |
| cache plateau | GPU 1系列、thumbnail 1系列 | GPU 403.586 MiB / thumbnail 180.277 MiB | 同左 | pass | commit `49d8aa1`。アプリ内予算とLunaPDF RSS。GPU driver側メモリは別計測していない |
